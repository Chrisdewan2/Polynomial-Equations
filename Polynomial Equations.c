#define PI 3.1416
#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    float a=1, b=0,c=-4,x;
    x=(-b+(sqrt(b*b-4*a*c))/(2*a);   printf("x= %f\n", x);
    a=1;b=0;c=-4;
    x=(-b-(sqrt(b*b-4*a*c))/(2*a);   printf("x= %f\n", x);


    return 0;
}
